<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="VolunteerListWindow" Height="450" Width="500"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">



    <!--External Grid-->
    <Grid >
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,1" EndPoint="1,1">
                <GradientStop Color="DarkBlue" Offset="0"/>
                <GradientStop Color="#E6F2FF" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>



        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>



        <!--Line of the combobox-->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10" VerticalAlignment="Center">
            <Label FontSize="12" Foreground="#D3D3D3" FontWeight="Bold" Content="Choose a type of call:" Margin="0,0,10,0" VerticalAlignment="Center" />
            <ComboBox ItemsSource="{Binding Source={StaticResource CallTypeCollectionKey}}" 
                      SelectedValue="{Binding Path=CallTypeSelected, Mode=TwoWay}" 
                      SelectionChanged="ComboBoxCallType_SelectionChanged" 
                      Width="200"/>
        </StackPanel>


        <!--DataGrid of the list of the volunteers-->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Path=VolunteerList}" SelectedItem="{Binding SelectedVolunteer}"
                MouseDoubleClick="lsvVolunteersList_MouseDoubleClick" IsReadOnly="True" AutoGenerateColumns="False"
                EnableRowVirtualization="True" RowDetailsVisibilityMode="VisibleWhenSelected" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding VolunteerId}" Header="Id of the Volunteer" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="Auto"/>
                <DataGridTemplateColumn Header="Is Active" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsEnabled="False" IsChecked="{Binding Path=IsActive}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Binding="{Binding SumOfCaredCall}" Header="Cared Calls" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding SumOfCancelledCall}" Header="Cancelled Calls" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding SumOfCallExpired}" Header="Expired Calls" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding CallId}" Header="Id of the current call" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding CallType}" Header="Type of the current call" Width="Auto"/>
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Click="DeleteButton_Click" Tag="{Binding VolunteerId}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>


        <Button Content="Add" Grid.Row="2" HorizontalAlignment="Center" Width="100" Margin="0,4,0,10" Click="AddButton_Click"/>

    </Grid>
</Window>

    


