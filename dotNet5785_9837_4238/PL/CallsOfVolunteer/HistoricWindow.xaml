<Window x:Class="PL.CallsOfVolunteer.HistoricWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.CallsOfVolunteer"
        mc:Ignorable="d"
        Title="HistoricWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid>

        <Grid.Background>
            <LinearGradientBrush StartPoint="0,1" EndPoint="1,1">
                <GradientStop Color="DarkBlue" Offset="0"/>
                <GradientStop Color="#E6F2FF" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Line of the combobox-->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="19,13,0,7" HorizontalAlignment="Left">
            <Label FontSize="14" Foreground="#D3D3D3" FontWeight="Bold" Content="Sort By:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox  ItemsSource="{Binding Source={StaticResource CloseCallInListSortCollection}}" 
                 SelectedValue="{Binding Path=SortSelected, Mode=TwoWay}"  
                 Width="150" 
                 SelectionChanged="ComboBoxSort_SelectionChanged" Margin="0,0,20,0"/>

            <Label FontSize="14" Foreground="#D3D3D3" FontWeight="Bold" Content="Filter By:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource CallTypeCollectionKey}}" 
                 SelectedValue="{Binding Path=CallTypeSelected, Mode=TwoWay}"
                 Width="150" 
                 SelectionChanged="ComboBoxFilter_SelectionChanged"/>
        </StackPanel>
      

        <!-- DataGrid for the list of calls -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding ListOfCalls}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  EnableRowVirtualization="True"
                  RowDetailsVisibilityMode="VisibleWhenSelected"
                   Margin="10,10,10,154" Grid.RowSpan="2">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding CallId}" Header="Call ID" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding CallType}" Header="Type" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Adress}" Header="Address" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding OpenTime}" Header="Open Time" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding StartTime}" Header="Start Time" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding FinishTime}" Header="Finish Time" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding TypeOfEnd}" Header="End Type" Width="Auto"/>
            </DataGrid.Columns>
            
        </DataGrid>

        <!-- Return Button -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Return to Your Dashboard" Click="ReturnButton_Click" Width="250" Height="50"/>
        </StackPanel>

    </Grid>



    <!--<Grid>
        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        --><!--grid for the elements of the calls--><!--
        <ListBox ItemsSource="{Binding ListOfCalls}" Grid.Row="2" Grid.ColumnSpan="2" Background="Transparent" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />

                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding CallId}" FontStyle="Italic" Grid.Column="0" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding CallType}" Grid.Column="1" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding Adress}" Grid.Column="2" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding OpenTime}" Grid.Column="3" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding StartTime}" Grid.Column="4" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding FinishTime}" Grid.Column="5" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding TypeOfEnd}" Grid.Column="5" HorizontalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox> 

        --><!--<Grid>
            <ListView ItemsSource="{Binding}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Call ID" DisplayMemberBinding="{Binding CallId}" />
                        <GridViewColumn Header="Adress" DisplayMemberBinding="{Binding Adress}" />
                        <GridViewColumn Header="Type" DisplayMemberBinding="{Binding CallType}" />
                        <GridViewColumn Header="End Type" DisplayMemberBinding="{Binding TypeOfEnd}" />
                        <GridViewColumn Header="OpenTime" DisplayMemberBinding="{Binding OpenTime}" />
                        <GridViewColumn Header="StartTime" DisplayMemberBinding="{Binding StartTime}" />
                        <GridViewColumn Header="FinishTime" DisplayMemberBinding="{Binding FinishTime}" />

                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>-->
        <!--<Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <Grid.RowDefinitions>

                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            -->
        <!-- ID -->
        <!--
            <Label Grid.Row="0" Grid.Column="0" Content="ID :" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CallId}" IsReadOnly="True"/>

            -->
        <!-- Type of call -->
        <!--
            <Label Grid.Row="1" Grid.Column="0" Content="Type d'appel :" VerticalAlignment="Center"/>
            <ComboBox Grid.Row="1" Grid.Column="1" SelectedValue="{Binding SelectedCall.CallType}" 
      ItemsSource="{Binding Source={StaticResource CallTypeCollectionKey}}"/>



            -->
        <!-- Adress -->
        <!--
            <Label Grid.Row="2" Grid.Column="0" Content="Adresse complète :" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedCall.Address}" IsReadOnly="True"/>

            -->
        <!-- open time-->
        <!--
            <Label Grid.Row="3" Grid.Column="0" Content="Temps d'ouverture :" VerticalAlignment="Center"/>
            <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding SelectedCall.OpenTime}" IsEnabled="False"/>

            -->
        <!-- StartTime-->
        <!--
            <Label Grid.Row="4" Grid.Column="0" Content="StartTime:" VerticalAlignment="Center"/>
            <DatePicker Grid.Row="4" Grid.Column="1" SelectedDate="{Binding SelectedCall.StartTime}" IsEnabled="False"/>

            -->
        <!-- Temps de fin réel -->
        <!--
            <Label Grid.Row="5" Grid.Column="0" Content="FinishTime :" VerticalAlignment="Center"/>
            <DatePicker Grid.Row="5" Grid.Column="1" SelectedDate="{Binding SelectedCall.FinishTime}" IsEnabled="False"/>

            -->
        <!-- Type de fin -->
        <!--
            <Label Grid.Row="6" Grid.Column="0" Content="TypeOfEnd:" VerticalAlignment="Center"/>
            <ComboBox Grid.Row="6" Grid.Column="1" SelectedValue="{Binding SelectedCall.TypeOfEnd}" 
      ItemsSource="{Binding Source={StaticResource TypeOfEndCollectionKey}}"/>
        </Grid>--><!--
    </Grid>-->


</Window>